
% set the right and left margin according to TUAS rules
\usepackage{geometry}
 \geometry{
 a4paper,
 left=1.58in,
 right=0.79in,
 top=1.18in,
 bottom=1.18in
 }



\usepackage{lmodern}
\usepackage{amssymb,amsmath}
\usepackage{ifxetex,ifluatex}
\usepackage{fixltx2e} % provides \textsubscript

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% TUAS font TODO: not actually arial
\renewcommand{\familydefault}{\sfdefault}


% use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
% use microtype if available
\IfFileExists{microtype.sty}{%
\usepackage{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\usepackage{hyperref}
\hypersetup{unicode=true,
            pdftitle={Overview of the Thesis Structure and Style},
            pdfauthor={Jarmo Kivekäs},
            pdfborder={0 0 0},
            breaklinks=true}
\urlstyle{same}  % don't use monospace font for urls
\IfFileExists{parskip.sty}{%
\usepackage{parskip}
}{% else
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
}
\setlength{\emergencystretch}{3em}  % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{3}
% Redefines (sub)paragraphs to behave more like sections
\ifx\paragraph\undefined\else
\let\oldparagraph\paragraph
\renewcommand{\paragraph}[1]{\oldparagraph{#1}\mbox{}}
\fi
\ifx\subparagraph\undefined\else
\let\oldsubparagraph\subparagraph
\renewcommand{\subparagraph}[1]{\oldsubparagraph{#1}\mbox{}}
\fi

\title{Overview of the Thesis Structure and Style}
\author{Jarmo Kivekäs}
\date{2016}

% TUAS: adding the logo to the title page uses this
\usepackage{graphicx}


%%%%%% footers and headers
% TUAS: add appropriate footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\rhead{\thepage}
\cfoot{}
\lhead{}
\lfoot{TUAS BACHELOR'S THESIS | Jarmo Kivekäs}
% TUAS guidlines call for no header or footer rules
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

%%%%%%% this is used in the abstract page to show the number of packages
% using the commen \pageref{LastPage}
\usepackage{lastpage}

%%%%%%% TUAS guidlines call of 1.5 em line spacing
\usepackage{setspace}
\onehalfspacing


%%%%%%% These guys allow to aling items in a lits with tabs
\newcommand{\itab}[1]{\hspace{0em}\rlap{#1}}
\newcommand{\tab}[1]{\hspace{.2\textwidth}\rlap{#1}}
