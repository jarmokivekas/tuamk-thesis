% 11 pt font according to TUAS rules
\documentclass[11pt]{article}


% set the right and left margin according to TUAS rules
\usepackage{geometry}
 \geometry{
 a4paper,
 left=1.58in,
 right=0.79in,
 top=1.18in,
 bottom=1.18in
 }



\usepackage{lmodern}
\usepackage{amssymb,amsmath}
\usepackage{ifxetex,ifluatex}
\usepackage{fixltx2e} % provides \textsubscript
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}



\else % if luatex or xelatex
  \ifxetex
    \usepackage{mathspec}
  \else
    \usepackage{fontspec}
  \fi
  \defaultfontfeatures{Ligatures=TeX,Scale=MatchLowercase}
\fi

% TUAS font TODO: not actually arial
% also need 1.5 em line height
\renewcommand{\familydefault}{\sfdefault}


% use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
% use microtype if available
\IfFileExists{microtype.sty}{%
\usepackage{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\usepackage{hyperref}
\hypersetup{unicode=true,
            pdftitle={Overview of the Thesis Structure and Style},
            pdfauthor={Jarmo Kivekäs},
            pdfborder={0 0 0},
            breaklinks=true}
\urlstyle{same}  % don't use monospace font for urls
\IfFileExists{parskip.sty}{%
\usepackage{parskip}
}{% else
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
}
\setlength{\emergencystretch}{3em}  % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{3}
% Redefines (sub)paragraphs to behave more like sections
\ifx\paragraph\undefined\else
\let\oldparagraph\paragraph
\renewcommand{\paragraph}[1]{\oldparagraph{#1}\mbox{}}
\fi
\ifx\subparagraph\undefined\else
\let\oldsubparagraph\subparagraph
\renewcommand{\subparagraph}[1]{\oldsubparagraph{#1}\mbox{}}
\fi

\title{Overview of the Thesis Structure and Style}
\author{Jarmo Kivekäs}
\date{2016}

% TUAS: adding the logo to the title page uses this
\usepackage{graphicx}

% TUAS: add appropriate footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\rhead{\thepage}
\lfoot{TUAS BACHELOR'S THESIS | Jarmo Kivekäs}
% TUAS guidlines call for no header or footer rules
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}



\begin{document}
% maketitle is used to create the default article title
% we use \begin{titlepage} instead.
% \maketitle


\begin{titlepage}
% commend in this title section based mostly on the wiki
% https://en.wikibooks.org/wiki/LaTeX/Title_Creation
    BACHELOR'S THESIS

    ELECTRONIC ENGINEERING

    NELEKS13

    2017

    {
        \addtolength{\leftskip}{0.886in}
    	\vspace{5cm}
        JARMO KIVEKÄS\par
    	% \includegraphics[width=0.15\textwidth]{img/tuas-logo}\par\vspace{1cm}
    	% {\scshape\LARGE Columbidae University \par}
    	% {\scshape\Large Final year project\par}
    	{\Huge TITLE OF THE THESISI HERE, IT CAN BE LONG\par}
    }
    \vfill
    {
        \centering
    	\includegraphics{img/tuas-logo}\par\vspace{1cm}
    }
\end{titlepage}

\rule{\textwidth}{\textheight}
